| Model              | Attributes                                                    | Methods                                                                                                                                                                                         |
| ------------------ | ------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **User**           | `_id`, `username`, `name`, `email`, `password`, `isAdmin`     | `createUser({username, name, email, password})`<br>`login({email, password}) → token`<br>`resetPassword({userId, newPassword})`                                                                 |
| **Board**          | `_id`, `title`, `description`, `createdBy`                    | `createBoard({title, description, createdBy})`<br>`updateBoard({boardId, title?, description?})`<br>`deleteBoard({boardId})`<br>`getBoards({userId})`                                           |
| **BoardMember**    | `_id`, `userId`, `boardId`, `roleId`                          | `addMember({boardId, userId, roleId})`<br>`updateRole({boardId, userId, roleId})`<br>`removeMember({boardId, userId})`<br>`getMembers({boardId})`                                               |
| **Role**           | `_id`, `name`, `description`                                  | `createRole({name, description?})`<br>`getRoles()`                                                                                                                                              |
| **Permission**     | `_id`, `name`, `description`                                  | `createPermission({name, description?})`<br>`getPermissions()`                                                                                                                                  |
| **RolePermission** | `roleId`, `permissionId`                                      | `addPermissionToRole({roleId, permissionId})`<br>`removePermissionFromRole({roleId, permissionId})`<br>`getPermissionsByRole({roleId})`                                                         |
| **Column**         | `_id`, `boardId`, `name`, `order`                             | `addColumn({boardId, name})`<br>`updateColumn({columnId, name?, order?})`<br>`deleteColumn({columnId})`<br>`getColumns({boardId})`                                                              |
| **Card**           | `_id`, `columnId`, `title`, `description`, `position`         | `createCard({columnId, title, description?})`<br>`updateCard({cardId, title?, description?, position?, columnId?})`<br>`deleteCard({cardId})`<br>`moveCard({cardId, newColumnId, newPosition})` |
| **CardMember**     | `cardId`, `userId`                                            | `assignMember({cardId, userId})`<br>`unassignMember({cardId, userId})`<br>`getAssignedMembers({cardId})`                                                                                        |
| **Comment**        | `_id`, `cardId`, `userId`, `parentId`, `content`, `createdAt` | `addComment({cardId, userId, content, parentId?})`<br>`getComments({cardId})`                                                                                                                   |
| **Label**          | `_id`, `boardId`, `name`, `color`                             | `createLabel({boardId, name, color})`<br>`getLabels({boardId})`                                                                                                                                 |
| **CardLabel**      | `cardId`, `labelId`                                           | `addLabel({cardId, labelId})`<br>`removeLabel({cardId, labelId})`<br>`getLabels({cardId})`                                                                                                      |
